const results = JSON.parse(localStorage.getItem('votes'));

const votes = [];
const lables = [];

results.array.forEach(element => {
    votes.push
});
const document.getElementById('results').textContent =JSON.stringify(results. 0, 2);

const